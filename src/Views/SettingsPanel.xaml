<UserControl x:Class="Flow.Launcher.Plugin.AliasFlow.Views.SettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / Toolbar -->
        <DockPanel Grid.Row="0" LastChildFill="True">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock FontSize="14" FontWeight="SemiBold" Text="{DynamicResource Settings_Title}" />
                <TextBlock Margin="0,3,0,0"
                           Opacity="0.8"
                           TextWrapping="Wrap"
                           Text="{DynamicResource Settings_Subtitle}" />
            </StackPanel>

            <!-- Right Buttons -->
            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <!-- CRUD -->
                <Button Content="{DynamicResource Settings_AddButton}" MinWidth="72" Margin="0,0,6,0" Click="Add_Click"
                        ToolTip="{DynamicResource Settings_AddTooltip}"/>
                <Button Content="{DynamicResource Settings_EditButton}" MinWidth="72" Margin="0,0,6,0" Click="Edit_Click"
                        ToolTip="{DynamicResource Settings_EditTooltip}"/>
                <Button Content="{DynamicResource Settings_DeleteButton}" MinWidth="72" Margin="0,0,12,0" Click="Delete_Click"
                        ToolTip="{DynamicResource Settings_DeleteTooltip}"/>

                <!-- Backup -->
                <Button Content="{DynamicResource Settings_ImportButton}" MinWidth="96" Margin="0,0,6,0" Click="Import_Click"
                        ToolTip="{DynamicResource Settings_ImportTooltip}"/>
                <Button Content="{DynamicResource Settings_ExportButton}" MinWidth="96" Click="Export_Click"
                        ToolTip="{DynamicResource Settings_ExportTooltip}"/>
            </StackPanel>
        </DockPanel>

        <!-- List -->
        <Border Grid.Row="2" BorderBrush="#33000000" BorderThickness="1" CornerRadius="6">
            <ListView x:Name="ItemsList"
                      ItemsSource="{Binding Items}"
                      SelectedItem="{Binding Selected, Mode=TwoWay}"
                      MouseDoubleClick="ItemsList_MouseDoubleClick"
                      KeyDown="ItemsList_KeyDown"
                      PreviewMouseWheel="ItemsList_PreviewMouseWheel"
                      ScrollViewer.CanContentScroll="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">

                <ListView.View>
                    <GridView AllowsColumnReorder="True">
                        <GridViewColumn Header="{DynamicResource Settings_TitleColumn}" Width="170" DisplayMemberBinding="{Binding Title}" />
                        <GridViewColumn Header="{DynamicResource Settings_DescriptionColumn}" Width="240" DisplayMemberBinding="{Binding Description}" />
                        <GridViewColumn Header="{DynamicResource Settings_TargetColumn}" Width="320" DisplayMemberBinding="{Binding Path}" />
                        <GridViewColumn Header="{DynamicResource Settings_HotkeyColumn}" Width="170" DisplayMemberBinding="{Binding Hotkey}" />
                        <GridViewColumn Header="{DynamicResource Settings_KeywordsColumn}" Width="260" DisplayMemberBinding="{Binding KeywordsDisplay}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <TextBlock Grid.Row="3"
                   Margin="2,8,2,0"
                   Opacity="0.7"
                   Text="{DynamicResource Settings_TipText}"/>
    </Grid>
</UserControl>
